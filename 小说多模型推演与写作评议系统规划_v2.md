# 多模型小说世界与人物推演系统：产品与技术规划

> 文档性质：需求说明、系统设计方向与开发计划  
> 暂定项目名：`Novel Council` / `Story Simulation Engine`  
> 当前运行目标：以独立核心 + Pi Extension 落地，日常使用入口是 Pi / PI WEB；后续才考虑通过 MCP 兼容其他宿主  
> 开发原则：先验证推演是否确实改善正文，再增加复杂状态管理和可视化界面

---

## 0. 平台定位与开发工具边界

本项目当前的**实际运行平台是 Pi**。

完成后的第一版应当以以下形式使用：

```text
用户在 Pi / PI WEB 中提出写作任务
        ↓
Pi 主模型按照 Skill 生成暂定 Scene Packet
        ↓
Pi Extension 调用本项目的独立推演核心
        ↓
推演核心并发调用世界、人物及其他评议模型 API
        ↓
结构化汇总返回 Pi 主模型
        ↓
Pi 主模型重新规划并写正文
```

VS Code、Cline、OpenCode、Codex IDE 等只用于**开发、阅读、修改、测试和调试本项目代码**，不是本项目的运行依赖，也不是最终用户使用本项目的入口。

尤其需要明确：

- Cline 只是当前可选的编码 Agent。
- 不得因为使用 Cline 开发，就把核心实现为 Cline SDK 应用。
- 不得要求最终用户安装或启动 Cline。
- 不得使用 Cline 的 Agent Teams、子代理或内部会话机制代替本项目自己的评议编排器。
- 不得把 API 配置保存在 Cline 专属设置中作为唯一数据源。
- 不得让核心代码依赖 VS Code 扩展 API。
- OpenCode、Codex、Trae 等同样只属于未来可选适配对象，不是第一版运行环境。
- 第一版必须在没有 Cline、OpenCode 和 VS Code 的服务器环境中，通过 Pi Extension 正常运行。
- 核心可以由 CLI 独立测试，但实际日常入口仍是 Pi / PI WEB。
- 后续增加 MCP 的目的只是提高可移植性，不能削弱 Pi 作为当前主入口的完整支持。

因此，编码模型在阅读本文档后，应把自己的角色理解为：

> 使用当前开发工具，为 Pi 开发一个宿主无关核心及 Pi Extension，而不是开发一个依托当前编码工具运行的应用。

---

## 1. 项目背景

长篇小说正文生成中，主模型往往能够理解已有设定和作者要求，却仍容易出现以下问题：

1. 人物只按当前场景功能行动，没有真正承接此前经历、身份、关系历史、认知局限和身体状态。
2. 人物的反应看似合理，实际缺乏个人性；换一个角色也能说出同样的话、采取同样的行动。
3. 世界只围绕主角和当前场景运转，场外人物、组织、制度、资源、时间、空间和消息传播没有持续产生影响。
4. 主模型先形成剧情方案后，容易围绕自己的第一判断自洽，而不是重新检查前提是否成立。
5. 为了完成作者指定的剧情节点，模型会跳过现实阻力、人物自保本能、关系阶段和信息差。
6. 单纯增加更长的提示词，往往只能增加规则数量，不能稳定产生真正独立的反思和推演。

本项目希望在“主模型正式写正文之前”，引入一个可配置的多模型推演与写作评议流程。多个独立子模型分别从世界运行、人物心理与经历、关系、行业或时代逻辑、结构和叙事等角度检查暂定方案，必要时互相评议，再把结构化结论交给主模型。主模型保留最终裁决权，并在评议后重新规划正文。

---

## 2. 核心目标

### 2.1 主要目标

系统应当实现以下完整流程：

1. 主模型根据作者要求和当前上下文，先形成一个**暂定场景方案**。
2. 主模型把规范化的 `Scene Packet` 交给推演系统。
3. 推演系统按照配置，启动一个或多个“评议组”。
4. 每个评议组可包含一个或多个成员模型；不同成员可以使用不同 API、模型和视角提示词。
5. 第一轮必须独立并发分析，成员不能看到其他成员的结论。
6. 当某组成员多于一个时，可按配置进行一轮匿名交叉评议。
7. 每组由主持模型或规则化汇总器生成组内报告。
8. 世界组、人物组及其他可选评议组的结果经过跨组冲突检查和总汇总。
9. 主模型收到压缩后的最终报告，重新判断原计划应当保留、修改还是推翻。
10. 主模型在完成二次规划后写正文。
11. 子模型提出的人物经历、秘密、关系解释和世界变化只能作为假设或提案，不能自动写入正式设定。

### 2.2 质量目标

系统的价值不是“调用更多模型”，而是提高以下质量：

- 人物反应具有经历连续性、身份约束和个人差异。
- 人物只依据自己真正知道的信息行动。
- 世界中的其他人和组织拥有自己的目标、节奏和行动。
- 交通、时间、资源、制度、行业规则、社会关系和消息传播具有约束力。
- 主模型能够看见原计划中的便利化、工具人化、关系越级和因果缺口。
- 评议结果包含可执行的替代路径，而不只是指出问题。
- 少数意见可以被保留，不以“多数模型一致”自动视为正确。

---

## 3. 非目标

第一阶段不做以下内容：

- 不自动生成完整小说正文。
- 不让子模型直接修改正式设定文件。
- 不让子模型直接提交世界状态变化。
- 不建立复杂数据库或知识图谱。
- 不自动拦截主模型的每一次普通回答。
- 不进行无限轮自由辩论。
- 不要求模拟完整社会或完整人物人生。
- 不直接模仿指定作家的具体文风。
- 不先开发复杂 WebUI。
- 不修改 Pi、OpenCode、Codex 等宿主框架的核心代码。

---

## 4. 设计原则

### 4.1 主模型拥有最终裁决权

子模型是评议者，不是作者，也不是设定裁判。它们可以否决暂定方案、提出替代方案和指出未知条件，但不能直接决定正史。

### 4.2 暂定方案不是既定答案

主模型第一次提出的方案必须标注为：

```json
{
  "provisional": true
}
```

所有评议者都被允许：

- `accept`：方案基本成立；
- `revise`：方案需要调整；
- `reject`：方案建立在错误或不足的前提上。

### 4.3 独立判断优先于自由讨论

所有成员第一轮必须隔离分析。自由聊天容易造成附和、意见同质化和上下文膨胀。只有独立意见生成后，才允许一轮匿名交叉评议。

### 4.4 事实、推论和创作建议必须分离

所有输出必须区分：

- `canonFacts`：正文或设定已经确认的事实；
- `inferences`：由事实推导出的判断；
- `hypotheses`：为解释人物或世界提出的可能性；
- `creativeSuggestions`：纯粹的剧情或塑造建议；
- `uncertainties`：证据不足、必须由作者或主模型裁决的部分。

### 4.5 子模型默认只读

世界模型、人物模型和其他评议成员只读取输入材料，并返回 `proposedDelta`。正式状态只能由单独的提交步骤更新。

### 4.6 报告压缩，原始记录留档

主模型不应接收全部讨论全文。系统保留原始报告供用户查看，但默认只将结构化总报告放入主模型上下文。

### 4.7 核心与宿主解耦

推演核心不应依赖 Pi、OpenCode、Codex 或某个 WebUI。宿主适配层只负责：

- 把任务传给核心；
- 展示进度和报告；
- 把最终结果返回主模型。

---

## 5. 总体工作流

```text
作者提出本章要求
        ↓
主模型读取正文、设定和最近状态
        ↓
主模型生成暂定 Scene Packet
        ↓
推演编排器
   ├─ 世界评议组
   ├─ 人物评议组
   └─ 可选写作评议组
        ↓
各成员第一轮独立并发分析
        ↓
可选：组内匿名交叉评议
        ↓
各组主持人汇总
        ↓
跨组前提与冲突协调
        ↓
生成 Final Council Report
        ↓
主模型明确裁决：
保留 / 局部修改 / 完全推翻 / 另提新方案
        ↓
主模型重新规划
        ↓
主模型写正文
        ↓
可选：正文后复核
        ↓
用户确认后提交正式状态变化
```

---

## 6. Scene Packet：主模型派发的任务包

主模型不能把整段会话直接扔给所有子模型。它需要先生成一个结构化任务包。

建议字段：

```json
{
  "schemaVersion": "1.0",
  "projectId": "项目标识",
  "chapterId": "章节或场景标识",
  "provisional": true,
  "authorRequest": "作者本次明确要求",
  "chapterGoal": "本章希望达成的作用",
  "provisionalPlan": [
    {
      "id": "beat-1",
      "summary": "暂定事件节点",
      "required": false,
      "reason": "为什么主模型暂时这样安排"
    }
  ],
  "timeAndPlace": {
    "time": "当前时间",
    "place": "当前地点",
    "elapsedSincePreviousScene": "距上一场过去多久"
  },
  "canonFacts": [],
  "worldStateSummary": [],
  "recentEvents": [],
  "characters": [
    {
      "name": "人物名",
      "identity": [],
      "confirmedHistory": [],
      "currentKnowledge": [],
      "misunderstandings": [],
      "currentGoals": [],
      "currentFears": [],
      "physicalState": [],
      "relationshipState": [],
      "knownConstraints": []
    }
  ],
  "authorConstraints": [],
  "forbiddenOutcomes": [],
  "openQuestions": [],
  "sourceReferences": [
    {
      "path": "正文或设定文件路径",
      "scope": "相关段落说明"
    }
  ]
}
```

### 6.1 Scene Packet 的责任边界

主模型必须尽量整理已知事实，但不能为填满字段而发明信息。无法确定的内容应放入 `openQuestions` 或标记为 `unknown`。

---

## 7. 可配置评议会

### 7.1 评议组

第一版至少支持：

1. `worldCouncil`：世界运行、社会因果、场外行动。
2. `characterCouncil`：人物经历、认知、欲望、关系与行为。
3. `writingCouncil`：可选，负责结构、叙事张力和场景表达，不负责替代前两组的因果判断。

未来可增加：

- 年代与社会生活组；
- 行业专业性组；
- 权力与政治关系组；
- 情感关系组；
- 悬疑逻辑组；
- 军事或技术合理性组。

### 7.2 评议成员

每个成员是一个独立配置单元：

```json
{
  "id": "character-psychology",
  "name": "人物心理与防御机制评议者",
  "provider": "anthropic",
  "model": "模型标识",
  "baseUrlEnv": "CHARACTER_API_BASE_URL",
  "apiKeyEnv": "CHARACTER_API_KEY",
  "rolePromptPath": "prompts/character-psychology.md",
  "temperature": 0.4,
  "timeoutMs": 120000,
  "enabled": true
}
```

### 7.3 “作家视角”成员

允许用户指定作家、理论传统或创作取向，但系统不应把它简化为文风模仿。配置应转换成分析维度：

```json
{
  "id": "writer-perspective-a",
  "displayName": "指定作家视角 A",
  "referenceName": "用户指定的作家或创作传统",
  "analysisFocus": [
    "人物欲望如何通过礼仪与言外之意表现",
    "身份和阶级如何限制选择",
    "关系中的误解如何逐步形成",
    "会质疑当前方案中的哪些便利化处理"
  ],
  "prohibitedTasks": [
    "模仿具体句式",
    "复制作品片段",
    "把审美偏好冒充事实"
  ]
}
```

同一个模型扮演多个视角可以作为低成本模式，但系统应明确：这不等同于多个真正独立模型。

---

## 8. 各组职责

### 8.1 世界评议组

世界组只检查外部因果和环境运行，不负责直接写文学正文。

必须分析：

- 时间是否足够；
- 空间、距离、交通和通信是否成立；
- 资源、权限、身份、制度和行业规则；
- 消息传播路径及信息差；
- 场外人物、组织和利益相关者的行动；
- 当前事件对其他地区或关系的连锁影响；
- 原计划依赖了哪些便利条件；
- 不采用原计划时，世界最自然的发展方向。

建议输出：

```json
{
  "verdict": "accept | revise | reject",
  "validPremises": [],
  "invalidPremises": [],
  "blockingConflicts": [],
  "externalPressures": [],
  "offscreenEvents": [],
  "informationFlow": [],
  "resourceAndInstitutionConstraints": [],
  "alternativeBeats": [],
  "proposedWorldDelta": [],
  "uncertainties": []
}
```

### 8.2 人物评议组

人物组不能只输出性格标签。它必须以人物的经历、认知边界和当前处境为基础推演。

必须分析：

- 人物真正知道什么；
- 人物不知道什么、误解了什么；
- 此刻想得到什么、害怕失去什么；
- 关系历史如何改变其观察和反应；
- 身份、责任、身体状态和社会成本；
- 会主动采取什么行动；
- 哪些行为会明显违背人物；
- 什么条件变化后才可能改变选择；
- 原计划是否把人物写成工具人；
- 是否存在更可信的反应链。

建议输出：

```json
{
  "verdict": "accept | revise | reject",
  "characterFindings": [
    {
      "name": "人物名",
      "canonFacts": [],
      "inferredMotives": [],
      "hypothesizedHistory": [],
      "knowledgeBoundary": [],
      "likelyPerception": [],
      "likelyActions": [],
      "unlikelyActions": [],
      "emotionalProgression": [],
      "relationshipEffects": [],
      "conditionsForChange": [],
      "uncertainties": []
    }
  ],
  "interactionConflicts": [],
  "alternativeBeats": [],
  "proposedCharacterDelta": []
}
```

### 8.3 写作评议组

写作组是可选项，主要关注：

- 场景中心是否明确；
- 冲突和选择是否真正由人物与世界产生；
- 是否过早解释；
- 信息释放顺序；
- 场景是否只有功能，没有生活和变化；
- 是否存在更有效的进入点、停顿点和转折点；
- 原计划与作者要求的气质是否一致。

写作组不能推翻已确认的事实，也不能用“更戏剧化”压过人物真实性。

---

## 9. 多成员讨论机制

### 9.1 第一轮：隔离并发分析

- 所有成员读取同一份 Scene Packet。
- 不得看到其他成员回答。
- 使用并发调用。
- 结果必须通过 JSON Schema 校验。

### 9.2 第二轮：匿名交叉评议

仅在以下条件下启用：

- 该组成员数大于一；
- 当前模式为 `deep`；
- 或主模型/用户明确要求讨论。

程序将第一轮结论去除模型身份，整理为观点 A、B、C，并要求成员：

- 指出赞同和反对之处；
- 说明分歧来自事实、推论还是审美；
- 检查自己是否需要修正；
- 尝试提出能兼容不同意见的新路径；
- 不得仅复述其他观点。

默认最多一轮，避免无限讨论。

### 9.3 组内主持人

主持人可以是：

- 该组指定的独立模型；
- 该组中的某个成员；
- 总主持模型；
- 在单成员模式下省略。

主持人职责是整理，不是重新自由发挥。它必须保留：

- 共识；
- 核心分歧；
- 少数意见；
- 证据强弱；
- 可执行建议；
- 未解决问题。

### 9.4 跨组协调

世界组与人物组的结论可能建立在不同前提上。协调器必须检查：

- 人物报告是否假设了人物已经知道某消息；
- 世界报告是否认定该消息尚未传播；
- 人物是否有进入现场、获取资源或采取行动的权限；
- 世界建议改变后，哪些人物推演失效；
- 两组替代方案能否同时成立；
- 是否需要定向重跑某个成员。

第一版只进行一次定向修正，不自动进入无限循环。

---

## 10. 运行模式与成本控制

### 10.1 `quick`

- 世界组 1 个成员；
- 人物组 1 个成员；
- 不进行交叉评议；
- 不额外调用主持模型；
- 用于普通章节和快速检查。

### 10.2 `standard`

- 每组 1—3 个成员；
- 第一轮独立并发；
- 各组主持人汇总；
- 一次总汇总；
- 不进行成员间讨论。

### 10.3 `deep`

- 每组多个成员；
- 第一轮独立分析；
- 一轮匿名交叉评议；
- 组内汇总；
- 跨组冲突协调；
- 总主持人报告；
- 适用于关键转折、人物关系变化和重大世界事件。

### 10.4 预算参数

配置中应支持：

```json
{
  "budget": {
    "maxTotalCalls": 12,
    "maxInputTokensPerMember": 30000,
    "maxOutputTokensPerMember": 5000,
    "maxDiscussionRounds": 1,
    "maxRetriesPerCall": 1,
    "stopOnBudgetExceeded": true
  }
}
```

---

## 11. 最终报告

交给主模型的 `Final Council Report` 应保持紧凑：

```json
{
  "sceneId": "标识",
  "overallVerdict": "accept | revise | reject",
  "planStrengths": [],
  "worldFindings": [],
  "characterFindings": [],
  "writingFindings": [],
  "crossDomainConflicts": [],
  "consensus": [],
  "minorityOpinions": [],
  "alternativePlans": [
    {
      "id": "A",
      "summary": "替代路径",
      "advantages": [],
      "risks": [],
      "requiredChanges": []
    }
  ],
  "uncertainHypotheses": [],
  "proposedDeltas": [],
  "recommendedRevision": [],
  "questionsForMainModel": []
}
```

主模型收到报告后必须显式输出工作结论（可写入工具记录，不必向用户暴露冗长内部推理）：

- 原计划保留哪些部分；
- 修改哪些部分；
- 拒绝哪些建议；
- 是否采用少数意见；
- 是否发现评议者都未提出的新方案；
- 最终采用的新场景计划。

之后才允许写正文。

---

## 12. 正史与状态安全

### 12.1 目录建议

```text
.story-engine/
├── config/
│   ├── councils.json
│   └── providers.example.json
├── canon/
│   ├── world-state.json
│   ├── timeline.jsonl
│   ├── relationships.json
│   └── characters/
├── checkpoints/
├── reports/
│   └── <chapter-id>/
├── prompts/
└── schemas/
```

### 12.2 状态提交规则

- Worker 不得修改 `canon/`。
- 每次推演只生成 `proposedDeltas`。
- 主模型写完正文后，可生成 `Commit Proposal`。
- 只有用户确认或明确的提交命令，才允许写入正式状态。
- 每次提交前创建 checkpoint。
- 重跑同一章节时，必须区分不同 run ID。
- 报告、正文文件和正式状态之间要有可追溯标识。

### 12.3 人物经历假设

子模型可以提出“这类反应可能来自某种过往”，但必须标记为：

```text
hypothesis
```

除非已有正文证据，否则不得自动转为 `confirmedHistory`。

---

## 13. API 与供应商设计

### 13.1 第一版协议

优先支持 OpenAI-compatible Chat Completions 或 Responses 风格接口，同时保留 provider adapter。

每个成员可分别配置：

- provider；
- base URL；
- API Key 环境变量名；
- model；
- timeout；
- temperature；
- max tokens；
- reasoning 参数；
- 额外 headers。

### 13.2 密钥安全

- API Key 只从环境变量或宿主安全凭据读取。
- 禁止把 Key 写入仓库、日志、报告和错误消息。
- 提供 `.env.example`，不提交 `.env`。
- 对外显示时只显示 provider、模型和 Key 是否已配置。
- 日志必须过滤 Authorization 和自定义认证头。

### 13.3 并发与失败处理

采用 `Promise.allSettled()` 或等价机制：

- 单个成员失败不能让所有结果丢失；
- 超时后取消对应请求；
- JSON 无效时最多做一次格式修复重试；
- 失败成员在报告中明确标记；
- 主持人不能把缺失结果当作反对或赞成；
- 当有效成员不足配置阈值时，返回“不足以形成评议”，不伪造汇总。

---

## 14. 核心架构

```text
novel-council/
├── packages/
│   ├── core/
│   │   ├── orchestrator
│   │   ├── provider-adapters
│   │   ├── council-runner
│   │   ├── discussion-runner
│   │   ├── report-merger
│   │   ├── conflict-coordinator
│   │   ├── schema-validation
│   │   └── state-store
│   ├── cli/
│   ├── mcp-server/
│   ├── pi-extension/
│   └── shared-types/
├── prompts/
├── schemas/
├── examples/
├── tests/
└── docs/
```

### 14.1 独立核心

核心提供：

```ts
simulateScene(scenePacket, config): Promise<FinalCouncilReport>
```

不得直接依赖 Pi、OpenCode 或 VS Code。

### 14.2 CLI

CLI 用于：

- 脱离宿主测试；
- 加载示例 Scene Packet；
- 验证配置；
- 查看调用日志；
- 重放某次报告；
- 做自动化测试。

### 14.3 MCP Server

核心稳定后提供 MCP 工具，例如：

- `simulate_scene`
- `validate_scene_packet`
- `list_council_profiles`
- `get_simulation_report`
- `propose_state_commit`

这样可被 OpenCode、Codex、VS Code、Trae 和其他支持 MCP 的客户端复用。

### 14.4 Pi Extension

Pi Extension 第一版注册：

- LLM 工具：`simulate_scene`
- 用户命令：`/scene-sim`
- 配置检查命令：`/scene-config`
- 最近报告命令：`/scene-report`

Pi Skill 负责规定主模型的完整调用顺序，但并发、模型选择、失败处理和汇总必须由核心执行。

---

## 15. Pi Skill 的职责

Skill 只描述主模型工作流程：

1. 阅读作者要求和相关文件。
2. 生成暂定 Scene Packet。
3. 检查 Scene Packet 是否把推测冒充事实。
4. 调用 `simulate_scene`。
5. 阅读最终报告。
6. 明确裁决：接受、修改或推翻。
7. 重新规划场景。
8. 写正文。
9. 不自动提交 proposed delta。
10. 当工具失败或结论不足时，向用户说明，而不是假装已经完成推演。

Skill 不应自己模拟多个 API，也不应靠提示词假装并发。

---

## 16. 是否需要修改 PI WEB

### 第一阶段：不需要

Pi Extension 注册的普通工具可以在 Pi 会话中运行。现有 PI WEB 只需显示：

- 工具已开始；
- 世界组和人物组的进度；
- 哪些成员成功或失败；
- 最终压缩报告。

### 第二阶段：可选浏览器插件

验证核心有效后，可增加 PI WEB Workspace 面板：

- 评议组配置；
- 成员与模型选择；
- 快速、标准、深度模式；
- 世界/人物/写作报告标签页；
- 原始意见与最终汇总；
- 运行历史和报告对比；
- 拟议状态变化的接受/拒绝；
- API 配置状态，但不显示密钥。

### 需要 fork PI WEB 的情况

只有当需要：

- 在生成按钮旁直接增加“推演后写作”；
- 将报告嵌入每条消息；
- 把推演状态与会话树或 reroll 精确绑定；
- 增加现有插件 API 不支持的服务端接口；

才考虑 fork PI WEB。第一版不应承担这项维护成本。

---

## 17. 参考项目 `DlSNlGHT/World`

该项目可作为以下方面的设计参考：

- 独立世界推演 API；
- 世界状态与变化记录；
- 推演前存档点；
- 世界事件、势力和影响链；
- 让世界在主角视线之外继续运行；
- 对重生成和历史状态的处理思路。

不应直接复制：

- SillyTavern 浏览器事件依赖；
- `window` 全局对象；
- IndexedDB 绑定；
- 酒馆聊天记录结构；
- 前端与核心逻辑耦合。

本项目应把这些思想重构为宿主无关的 TypeScript 核心。

参考地址：

- https://github.com/DlSNlGHT/World

---

## 18. 开发阶段

### 阶段 0：架构审查

交付：

- `architecture.md`
- `data-contracts.md`
- `implementation-plan.md`
- 风险清单
- 需要用户确认的开放问题

此阶段不写大量实现代码。

### 阶段 1：最小原型

范围：

- 世界组一个成员；
- 人物组一个成员；
- 两个 API 并发；
- JSON Schema 校验；
- CLI 输入与输出；
- 无状态，只返回报告。

验收：

- 两个模型可使用不同 API；
- 单侧失败仍返回另一侧结果；
- 不泄露密钥；
- 输出能让主模型重新规划。

### 阶段 2：可配置多成员评议

范围：

- 每组任意成员数；
- 独立并发第一轮；
- 主持人汇总；
- quick / standard / deep；
- 报告保存；
- 成本与调用次数限制。

### 阶段 3：交叉评议与跨组协调

范围：

- 一轮匿名组内评议；
- 前提冲突检测；
- 定向重跑；
- 少数意见保留；
- 总主持报告。

### 阶段 4：Pi 集成

范围：

- Pi Extension；
- Pi Skill；
- `/scene-sim`；
- 在 PI WEB 中实测；
- 不修改 PI WEB。

### 阶段 5：MCP 与多宿主

范围：

- MCP Server；
- OpenCode 接入；
- Codex 接入；
- VS Code/Trae 接入测试。

### 阶段 6：状态管理

前提：必须先证明推演确实改善正文。

范围：

- canon；
- proposed delta；
- checkpoint；
- 用户确认提交；
- 重跑和报告版本关联。

### 阶段 7：可视化

范围：

- PI WEB 或独立 Web 面板；
- 配置和报告管理；
- 运行进度；
- 历史对比；
- 状态提案审批。

---

## 19. 测试要求

至少覆盖：

1. 两个成员并发成功。
2. 一个成员超时。
3. 一个成员返回无效 JSON。
4. 格式修复重试成功和失败。
5. 一个评议组没有有效结果。
6. 主持模型失败时仍保留原始成员报告。
7. 不同成员意见冲突。
8. 世界报告与人物报告前提冲突。
9. 单成员模式跳过讨论。
10. 预算上限触发。
11. API Key 不出现在日志和异常中。
12. 子模型尝试把假设写成正史时被校验器拒绝。
13. 同一 Scene Packet 重跑生成不同 run ID。
14. 报告压缩后仍能追溯原始意见。
15. Pi Extension 在 PI WEB 会话中可正常调用。

---

## 20. 验收标准

第一版被视为有效，不是因为“调用成功”，而是因为在真实章节测试中达到以下结果：

- 至少发现主模型初版未注意到的世界或人物问题；
- 能说明问题为何不可信；
- 能给出一个以上可执行替代方案；
- 主模型在读取报告后确实修改了原方案；
- 修改后的正文人物反应更有差异和连续性；
- 没有把推测自动写成正式设定；
- 调用成本和等待时间在可接受范围内；
- 部分 API 失败时，整个写作流程仍可继续。

建议用同一场景做对照测试：

1. 主模型直接写；
2. 单模型推演后写；
3. 多模型标准模式后写；
4. 深度评议后写。

由用户比较人物真实性、世界运行感、剧情自然度和语言质量。

---

## 21. 开发前需要确认的问题

1. 第一版准备使用哪些 API 和模型？
2. 世界组、人物组各放几个成员？
3. 是否需要作家视角成员，第一批使用哪些分析视角？
4. 主持人使用独立模型还是复用成员模型？
5. 默认运行模式是 quick 还是 standard？
6. 单次推演可接受的最长等待时间和最高成本是多少？
7. Scene Packet 从哪些小说文件中读取资料？
8. 第一版是否完全不写状态文件，只保存报告？
9. 报告是否需要用户可读的 Markdown 版和机器可读 JSON 版同时保存？
10. 是否先只支持 OpenAI-compatible API？
11. Pi Extension 是项目级安装还是全局安装？
12. 是否需要让主模型每次写正文前自动调用，还是由用户手动触发？

---

## 22. 给编码模型的启动指令

请先阅读本规划，不要立即大规模生成代码。

第一步应当：

1. 检查当前开发环境、Node.js、TypeScript 和目标 Pi 版本。
2. 阅读 Pi Extension、Skill、RPC 和示例扩展的当前官方文档。
3. 阅读 OpenCode 的插件、自定义工具、Agent Skill 和 MCP 文档。
4. 阅读 `https://github.com/DlSNlGHT/World`，只提取可复用的设计思想。
5. 指出本规划中不合理、过度设计或存在歧义之处。
6. 提出至少两种可行架构，并说明取舍。
7. 先生成：
   - `architecture.md`
   - `data-contracts.md`
   - `implementation-plan.md`
   - `risk-register.md`
8. 等用户确认后，再实现阶段 1。

硬性要求：

- 当前最终运行入口是 Pi / PI WEB，Cline 或其他编码 Agent 仅用于开发；
- 不得引入 Cline SDK、VS Code API 或其他开发工具专属运行依赖，除非用户以后单独批准可选适配层；
- 核心与宿主解耦；
- 不修改 Pi 和 PI WEB 内核；
- 第一版不实现数据库和复杂 WebUI；
- API Key 不进入源码、仓库和日志；
- 子模型只返回提案，不写正式设定；
- 第一轮成员分析必须独立并发；
- 使用结构化 schema；
- 单侧失败不能导致全局失败；
- 所有推演结果可追溯；
- 不猜测当前依赖包名和旧版 API，先读取实际文档和安装版本。

---

## 23. 推荐开发环境

建议把“开发工具”和“项目运行平台”严格分开。

### 当前建议的开发组合

**VS Code + Cline**

它们只负责：

- 查看和编辑代码；
- 阅读规划文档；
- 制订实现方案；
- 运行测试和命令；
- 查看 Git diff；
- 协助开发独立核心和 Pi Extension。

它们不负责：

- 充当最终小说推演系统；
- 代替项目自己的多模型编排器；
- 成为运行时依赖；
- 保存项目唯一的模型配置；
- 要求最终用户通过 Cline 使用本项目。

即使由 Cline 完成全部编码，最终第一版也必须能够在服务器上由 Pi / PI WEB 调用，而无需安装或运行 Cline、VS Code 或 Cline SDK。

### 其他可用开发工具

也可以使用 OpenCode、Codex IDE 扩展或其他编码 Agent。选择哪一个只影响开发体验，不应改变项目架构。

多供应商推演必须通过项目自身的 provider adapter 执行；不得依赖开发工具内部的子代理、Agent Teams 或专属多模型机制。

### 当前交付重点

第一版的宿主集成为：

```text
独立 TypeScript 核心
        +
Pi Extension
        +
Pi Skill
```

MCP、OpenCode、Codex、Trae 等适配属于后续兼容性工作，不是第一版的运行前提。

### 不建议的起点

- 直接在现有 PI WEB 生产环境中开发；
- 一开始 fork PI WEB；
- 一开始只写一个很长的 Skill；
- 一开始把世界状态、人物记忆、评议会和前端全部实现；
- 让编码模型直接在小说项目目录里边试边改。

建议新建独立 Git 仓库开发，完成 CLI 和测试后，再接入 Pi。
