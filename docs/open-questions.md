# Novel Council 开放问题

> 状态：v0.3（2026-08-05：阶段 2 决定 D23–D29 结案/登记）
> 已关闭问题见 `decision-log.md`（D1–D29）。本文件只登记**仍待决定/验证**的事项，按解决时点分组。

---

## A. 阶段 4 待验证：Pi 集成事实（D4）

> 验证方式约束：只读当时实际版本的 Pi 官方文档 + 最小本地验证；**不连 VPS、不检查或修改远程 Pi 安装**。当前不得自行假定答案。

| # | 问题 | 影响 |
|---|---|---|
| Q01 | Pi Extension 的打包/安装格式：能否随扩展部署普通 JS 库（`tsc` 产物 + `zod`）？是否需要 bundler？ | D5 连接方式终定；D11 bundler 解禁与否 |
| Q02 | LLM 工具的注册签名、参数与返回值约定 | `hosts/pi` 的接口设计 |
| Q03 | Pi 工具调用的超时上限：quick 模式（两次并发 API 调用，成员 timeout 默认 120s）是否会被宿主提前中断？ | 成员 timeout 默认值；连接方式 A/B 选择（R02） |
| Q04 | PI WEB 能否展示工具进行中的进度/分组状态？ | ProgressEvent 是否透传（core 已解耦，不依赖该能力） |
| Q05 | Pi Extension 项目级安装还是全局安装？（用户列为暂缓项） | 阶段 4 安装与部署文档 |

## B. 阶段 1 完成后（真实 API 质量评估前）

| # | 问题 | 影响 |
|---|---|---|
| Q06 | 人工质量评估（B01–B03）使用哪两个 OpenAI-compatible 端点/模型？（由用户手动执行；开发过程不连真实 API） | examples 占位配置替换；B 组评估启动 |

## C. 阶段 2 相关（Q07/Q08 已结案）

| # | 问题 | 状态 |
|---|---|---|
| Q07 | standard/deep 的成员数量与各自预算默认值 | **已结案（D26）**：成员数以配置为准；预算保持显式硬顶 + 预估提示，不按模式隐式改默认；deep 预算随阶段 3 管线再定 |
| Q08 | 主持模型：独立模型还是复用成员模型 | **已结案（D25）**：两者皆支持（`useMember` 复用 / 内联独立配置），默认不配置主持 |
| Q09 | 是否需要用户可读的 Markdown 版报告（与 JSON 双格式） | 开放；阶段 2 不做 |
| Q10 | 是否引入作家视角成员，第一批分析视角 | 开放；阶段 2 不做 |
| Q15 | 报告自动留档目录设计（原阶段 2 范围，已移出） | 开放；之后单独设计与实施（D23） |

## D. 阶段 3 前

| # | 问题 |
|---|---|
| Q11 | 交叉评议细节：匿名观点组织方式、是否恒为单轮 |

## E. 阶段 5–7 前（远期，仅登记）

| # | 问题 |
|---|---|
| Q12 | MCP Server 的启动时机与工具集 |
| Q13 | canon / proposedDelta 提交流程与 checkpoint 设计（阶段 6，以"推演已验证改善正文"为前提） |
| Q14 | 可视化形态：PI WEB 面板还是独立 Web（阶段 7） |

---

## 解决记录

| 问题 | 解决 | 日期 |
|---|---|---|
| 架构形态（A/B） | D1：单包模块化，当前不采用 monorepo | 2026-08-05 |
| 目标 Node 版本 | D3：Node.js 22 | 2026-08-05 |
| Pi 连接方式 | D5：A（import 库）暂定优先，B（CLI 子进程）备选，阶段 4 终定；不做本地 HTTP 首选 | 2026-08-05 |
| 阶段 1 provider | D6：仅 OpenAI-compatible；测试仅 Mock；开发不连真实 API | 2026-08-05 |
| 报告落盘 | D7：stdout 默认 + `--output`；不自动建 reports 目录 | 2026-08-05 |
| 输入/报告体积 | D8/D9：字符计；maxInputChars 50000 / maxReportChars 20000；报错不静默截断 | 2026-08-05 |
| 阶段 1 预算 | D10：quick only；maxTotalCalls 4 | 2026-08-05 |
| 依赖 | D11：typescript / vitest / zod / tsx；无 bundler、无 SDK、无框架 | 2026-08-05 |
| 隐私 | D12：用户知情接受；五条系统约束 | 2026-08-05 |
| 触发方式 | D13：手动 /scene-sim 或 Skill 显式调用 | 2026-08-05 |
| 阶段 2 范围与边界 | D23：多成员/主持/standard/重试分类/预算预估；不含 deep、writingCouncil、报告留档、Pi | 2026-08-05 |
| deep 阶段 2 处理 | D24：schema 可解析，orchestrator 显式 CONFIG_INVALID，不静默降级 | 2026-08-05 |
| 主持形态（Q08） | D25：useMember 复用或内联独立配置，默认无主持；FinalCouncilReport 保持规则合并 | 2026-08-05 |
| 阶段 2 预算纪律（Q07） | D26：显式硬顶 + 预估提示，不隐式改默认 | 2026-08-05 |
| 传输重试 | D27：provider 分类、runner 策略、禁止厂商特判 | 2026-08-05 |
| 并发测试方法 | D28：计数式断言（active/maxActive），禁墙钟阈值 | 2026-08-05 |
| 阶段 2 验收门槛 | D29：B10 真实小说材料质量评估（用户手动） | 2026-08-05 |
