# Novel Council 风险登记册

> 状态：v0.3（2026-08-05：阶段 2 风险登记）
> 可能性/影响：低 / 中 / 高。状态：开放 / 监控 / 已设计缓解 / 已接受 / 已关闭。

| ID | 风险 | 类别 | 可能性 | 影响 | 缓解 | 状态 |
|---|---|---|---|---|---|---|
| R01 | Pi Extension 打包/安装格式未知，连接方式（D5）与 bundler 决策受阻 | 集成 | 中 | 高 | 阶段 4 读官方文档 + 最小本地验证；A/B 两方案并行预备；不预先假定（D4） | 开放（Q01） |
| R02 | Pi 工具调用超时上限未知，quick 推演（两次 API 等待）可能超限 | 集成 | 中 | 高 | 阶段 4 验证（Q03）；成员 timeout 可配置；必要时改用连接方式 B | 开放（Q03） |
| R03 | PI WEB 进度展示能力未知 | 集成 | 中 | 低 | ProgressEvent 与宿主解耦，宿主可忽略（core 不依赖展示能力） | 开放（Q04） |
| R04 | OpenAI-compatible 端点不一定支持结构化输出，JSON 可靠性差 | 技术 | 高 | 中 | 提示词约束 + 首个 JSON 块提取 + zod 校验 + 一次修复重试；失败标记 `REPAIR_FAILED`，不伪造结果 | 已设计缓解 |
| R05 | 内联政策与 `maxInputChars`（50000）存在张力，长章节易超限 | 契约 | 中 | 中 | 明确报错 `PACKET_TOO_LARGE`（D8）；阶段 4 由 Skill 指导主模型精选摘录；token 计量后续阶段再议 | 已设计缓解 |
| R06 | API Key 泄漏进日志/错误/报告 | 安全 | 低 | 高 | env-only；redaction（认证头 + 密钥字面量）；A11 自动化断言 | 已设计缓解 |
| R07 | 手稿摘录外发第三方 API 的隐私问题 | 合规 | — | 用户已知情接受（D12） | README/配置文档警示；只发启用成员；无隐藏调用；无后台上传 | 已接受（残留：T11 文档义务） |
| R08 | 自动化测试误连真实 API | 流程 | 低 | 中 | 测试只注入 Mock fetch（D6）；CI 无密钥环境；A18/A19 断言不发起意外调用 | 已设计缓解 |
| R09 | zod 契约与 JSON Schema 双份维护漂移 | 维护 | 中 | 中 | zod 单一来源，`schemas/` 构建生成，禁止手写 | 已设计缓解 |
| R10 | 单包边界随时间松弛，宿主代码渗入 core | 维护 | 中 | 高 | 依赖方向规则 + code review；迁移触发条件已定义（D1） | 监控 |
| R11 | 主模型不读报告/不显式裁决/把假设写进正文——core 无法强制 | 产品 | 中 | 中 | 阶段 4 Skill 软约束；属人工评估项（B04/B06），不冒充自动化保证 | 已接受 |
| R12 | quick 规则化 merger 无语义归纳能力，报告价值可能有限 | 产品 | 中 | 中 | 阶段 2 主持 merger 补足；B01–B03 价值门槛未过则不扩展（规划原则） | 监控 |
| R13 | 开发机 Node 26 与目标 Node 22 的 API 差异 | 环境 | 低 | 低 | 只用 Node 22 可用 API；tsconfig 按 Node 22；必要时 nvm 实测 | 监控 |
| R14 | 规划 §10.4 预算示例与 deep 管线矛盾 | 规划 | — | — | 已由 D10/D14 消解：阶段 1 仅 quick（maxTotalCalls 4）；standard/deep 预算阶段 2/3 定义 | 已关闭 |
| R15 | `maxReportChars` 裁剪丢失关键意见 | 产品 | 低 | 中 | 优先级裁剪保 findings（blocking 优先）；`truncation.droppedSections` 可见可追溯 | 已设计缓解 |
| R16 | quick 仅 2 个成员，匿名交叉评议无从谈起，价值验证样本偏薄 | 产品 | 中 | 低 | 阶段 1 定位即最小验证；阶段 2 放开多成员后样本加厚；B10 复评 | 已接受 |
| R17 | 多成员 + 主持配置下 `maxTotalCalls` 旧默认值（4）必然偏小，用户易触顶 | 契约 | 高 | 低 | `estimateCalls` 预估 + check-config/simulate 提示；不隐式改默认（D26） | 已设计缓解 |
| R18 | 主持模型失败或输出不可用导致组汇总缺失 | 技术 | 中 | 中 | `fallbackUsed` 规则化回退；原始成员报告恒保留（A06/A33）；主持不介入 FinalCouncilReport（D25） | 已设计缓解 |
| R19 | 传输重试放大成本与延迟 | 技术 | 低 | 低 | 有界 `maxTransportRetries`（默认 1）、计入预算、仅对可重试分类生效（D27） | 已设计缓解 |
| R20 | `councilId` 硬编码（world 以外静默按 character 处理）在多成员/多组下误分类 | 维护 | 中 | 中 | C2 引入 council-kinds 注册表；未知组 id → `CONFIG_INVALID`（D23） | 已设计缓解（C2 落地后关闭） |
